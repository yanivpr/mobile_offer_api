<% if @offers.blank? %>
  <div class='no_offers'>
    No offers
  </div>
<% else %>
  <h1>Received Offers:</h1>

  <ul>
    <% @offers.each do |offer| %>
      <%= render 'offer', offer: offer %>
    <% end %>
  </ul>
<% end %>

<%= link_to 'Make another offers request', root_path %>
